# Next.js reads env files from this folder (frontend/), not the repo root .env.
# Copy this to frontend/.env.local if you need to point the frontend at a
# backend that isn't running on localhost:4000 (e.g. a deployed backend URL).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
